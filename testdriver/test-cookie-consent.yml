version: 4.1.30
steps:
  - prompt: Focus the browser window.
    commands: []
  - prompt: Click the "Accept" button in the cookie consent banner.
    commands:
      - command: hover-text
        text: Accept
        description: accept button in the cookie consent banner
        action: click
  - prompt: >-
      Assert that the cookie consent banner disappears.  # Use the content of
      the file from the environment
    commands:
      - command: assert
        expect: the cookie consent banner is not visible
